template:
  quot_rule: |
    <rule>
      Bắt buộc thêm dấu nháy kép (") bao quanh tên schema, tên bảng, tên trường, bí danh.
      <note>
        1. Dấu chấm (.) không được nằm trong dấu nháy, phải viết thành "schema"."table"
        2. Ngay cả khi định danh không chứa ký tự đặc biệt hoặc không phải từ khóa, vẫn bắt buộc thêm dấu nháy kép
        3. Dameng mặc định chuyển định danh không có dấu nháy thành chữ hoa
      </note>
    </rule>

  limit_rule: |
    <rule>
      Khi cần giới hạn số dòng, phải dùng cú pháp tương thích Dameng
      <note>
        1. Cách viết chuẩn: LIMIT [count]
        2. Cách viết phân trang: LIMIT [offset], [count] hoặc LIMIT [count] OFFSET [start]
        3. Tương thích cách viết Oracle: WHERE ROWNUM <= 100 (cần Dameng phiên bản 7+)
      </note>
    </rule>

  other_rule: |
    <rule>Phải sinh bí danh cho mỗi bảng (không thêm AS)</rule>
    {multi_table_condition}
    <rule>Cấm dùng dấu sao (*), phải nêu rõ tên trường</rule>
    <rule>Trường tiếng Việt/ký tự đặc biệt cần giữ nguyên tên gốc và thêm bí danh tiếng Anh</rule>
    <rule>Trường hàm phải thêm bí danh</rule>
    <rule>Trường phần trăm dùng TO_CHAR(x*100,'990.99') || '%'</rule>
    <rule>Tránh xung đột với từ khóa Dameng (như LEVEL/ORDER/PARTITION, v.v.)</rule>

  basic_example: |
    <basic-examples>
      <intro>
        📌 Các ví dụ sau tuân thủ nghiêm ngặt chuẩn Dameng (DM) trong <Rules>, minh họa cách viết SQL đúng yêu cầu và các trường hợp lỗi điển hình.
        ⚠️ Lưu ý: tên bảng, tên trường trong ví dụ đều là hư cấu để minh họa, khi dùng thực tế cần thay bằng định danh thật do người dùng cung cấp.
        🔍 Điểm cần quan sát kỹ:
          1. Cách dùng chuẩn dấu nháy kép bao quanh mọi đối tượng cơ sở dữ liệu
          2. Cách xử lý các trường đặc biệt như bí danh Việt-Anh/phần trăm/hàm
          3. Cách tránh xung đột từ khóa
      </intro>
      <example>
        <input>Truy vấn 100 đơn hàng đầu của bảng TEST.ORDERS (có trường tiếng Việt và phần trăm)</input>
        <output-bad>
          SELECT * FROM TEST.ORDERS LIMIT 100  -- Lỗi: chưa thêm dấu nháy, dùng dấu sao
          SELECT "Mã đơn hàng", "Số tiền" FROM "TEST"."ORDERS" "t1" WHERE ROWNUM <= 100  -- Lỗi: lẫn lộn ROWNUM và LIMIT
          SELECT COUNT("Mã đơn hàng") FROM "TEST"."ORDERS" "t1"  -- Lỗi: hàm chưa thêm bí danh
        </output-bad>
        <output-good>
          SELECT
            "t1"."Mã đơn hàng" AS "order_id",
            "t1"."Số tiền" AS "amount",
            COUNT("t1"."Mã đơn hàng") AS "total_orders",
            TO_CHAR("t1"."Tỷ lệ chiết khấu" * 100, '990.99') || '%' AS "discount_percent"
          FROM "TEST"."ORDERS" "t1"
          GROUP BY "t1"."Mã đơn hàng", "t1"."Số tiền", "t1"."Tỷ lệ chiết khấu"
          LIMIT 100
        </output-good>
      </example>

      <example>
        <input>Thống kê tỷ lệ hoạt động của bảng người dùng PUBLIC.USERS (có trường từ khóa LEVEL)</input>
        <output-bad>
          SELECT LEVEL, status FROM PUBLIC.USERS  -- Lỗi: chưa xử lý từ khóa
          SELECT "LEVEL", ROUND(active_ratio*100) FROM "PUBLIC"."USERS"  -- Lỗi: định dạng phần trăm sai
        </output-bad>
        <output-good>
          SELECT
            "u"."LEVEL" AS "user_level",
            TO_CHAR("u"."active_ratio" * 100, '990.99') || '%' AS "active_percent"
          FROM "PUBLIC"."USERS" "u"
          WHERE "u"."status" = 1
          LIMIT 0, 1000  -- Cách viết tương thích Dameng
        </output-good>
      </example>
    </basic-examples>

  example_engine: DM Database 8
  example_answer_1: |
    {"success":true,"sql":"SELECT \"country\" AS \"country_name\", \"continent\" AS \"continent_name\", \"year\" AS \"year\", \"gdp\" AS \"gdp_usd\" FROM \"Sample_Database\".\"sample_country_gdp\" ORDER BY \"country\", \"year\"","tables":["sample_country_gdp"],"chart-type":"line"}
  example_answer_1_with_limit: |
    {"success":true,"sql":"SELECT \"country\" AS \"country_name\", \"continent\" AS \"continent_name\", \"year\" AS \"year\", \"gdp\" AS \"gdp_usd\" FROM \"Sample_Database\".\"sample_country_gdp\" ORDER BY \"country\", \"year\" LIMIT 1000","tables":["sample_country_gdp"],"chart-type":"line"}
  example_answer_2: |
    {"success":true,"sql":"SELECT \"country\" AS \"country_name\", \"gdp\" AS \"gdp_usd\" FROM \"Sample_Database\".\"sample_country_gdp\" WHERE \"year\" = '2024' ORDER BY \"gdp\" DESC","tables":["sample_country_gdp"],"chart-type":"pie"}
  example_answer_2_with_limit: |
    {"success":true,"sql":"SELECT \"country\" AS \"country_name\", \"gdp\" AS \"gdp_usd\" FROM \"Sample_Database\".\"sample_country_gdp\" WHERE \"year\" = '2024' ORDER BY \"gdp\" DESC LIMIT 1000","tables":["sample_country_gdp"],"chart-type":"pie"}
  example_answer_3: |
    {"success":true,"sql":"SELECT \"country\" AS \"country_name\", \"gdp\" AS \"gdp_usd\" FROM \"Sample_Database\".\"sample_country_gdp\" WHERE \"year\" = '2025' AND \"country\" = 'Việt Nam'","tables":["sample_country_gdp"],"chart-type":"table"}
  example_answer_3_with_limit: |
    {"success":true,"sql":"SELECT \"country\" AS \"country_name\", \"gdp\" AS \"gdp_usd\" FROM \"Sample_Database\".\"sample_country_gdp\" WHERE \"year\" = '2025' AND \"country\" = 'Việt Nam' LIMIT 1000","tables":["sample_country_gdp"],"chart-type":"table"}
